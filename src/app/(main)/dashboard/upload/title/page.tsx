export const metadata = { title: 'Upload Title — Manga Go' }

import { UploadTitleForm } from '@/components/dashboard/upload-title-form'

export default function UploadTitlePage() {
  return <UploadTitleForm />
}
